# Configuration values for PostgreSQL

replicaCount: 1

image:
  repository: postgres
  tag: 15-alpine
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 5432
  targetPort: 5432

db:
  user: postgres
  password: postgres
